node_modules/
dist/*
!dist/action/
dist/action/*
!dist/action/index.js
coverage/
.play-review-notify/
*.log
.env
.env.*
.DS_Store
# Raw, unredacted Play Console emails collected for Phase 0 (never commit)
test/fixtures/email/private/
# Raw Play API snapshots collected for Phase 0 (never commit)
test/fixtures/play-api/private/
# Local consumer config used for manual testing (contains only ${ENV} references, but keep it out of the tool repo)
/play-review-notify.yml
# Local-only notes and scratch files
/.local/
