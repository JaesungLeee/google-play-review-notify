# Dependency updates. Runtime dependency bumps use a `fix(deps)` prefix so Release Please ships
# them as a patch release; development and Actions bumps use `chore`/`ci` and do not trigger one.
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: '09:00'
      timezone: Asia/Seoul
    open-pull-requests-limit: 5
    commit-message:
      prefix: 'fix(deps)'
      prefix-development: 'chore(deps)'
    groups:
      runtime-minor:
        dependency-type: production
        update-types: [minor, patch]
      dev-minor:
        dependency-type: development
        update-types: [minor, patch]
    ignore:
      # Keep the pinned Node.js major of the Action runtime (action.yml) in sync manually.
      - dependency-name: '@types/node'
        update-types: ['version-update:semver-major']
      # vitest 5 requires Node.js 22.12+; the test suite still runs on Node.js 20 in CI.
      - dependency-name: 'vitest'
        update-types: ['version-update:semver-major']
      # typescript-eslint supports TypeScript < 6.1 only. Revisit when it supports 7.
      - dependency-name: 'typescript'
        update-types: ['version-update:semver-major']
      # zod 4 changes .default() and z.record() semantics; migrate deliberately, not via a bump.
      - dependency-name: 'zod'
        update-types: ['version-update:semver-major']

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: '09:00'
      timezone: Asia/Seoul
    commit-message:
      prefix: 'ci(deps)'
    groups:
      actions:
        patterns: ['*']
